apiVersion: v1
kind: Namespace
metadata:
  name: ${formation}
---
apiVersion: v1
kind: Secret
metadata:
  namespace: ${formation}
  name: cloud66-registry-pull-secret
data:
  .dockerconfigjson: ${env("REGISTRY_AUTH")}
type: kubernetes.io/dockerconfigjson
