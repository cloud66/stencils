apiVersion: v1
kind: Service
metadata:
  namespace: ${namespace}
  name: ${concat(service, "-svc")}
  labels:
    app: ${namespace}
    tier: ${service}
spec:
#  ports:
#  - port: EXTERNAL_PORT
#    targetPort: INTERNAL_PORT
  selector:
    app: ${namespace}
    tier: ${service}
